# Database Configuration
# SingleStore database connection string
# Format: mysql://username:password@host:port/database?ssl-ca=path&sslaccept=strict
DATABASE_URL="mysql://username:password@host:port/database?ssl-ca=./certs/singlestore_bundle.pem&sslaccept=strict"

# GitHub OAuth Application
# Create a GitHub OAuth App at: https://github.com/settings/applications/new
# Homepage URL: http://localhost:3000 (development) or https://yourdomain.com (production)
# Authorization callback URL: http://localhost:3000/api/auth/callback/github (development)
#                            https://yourdomain.com/api/auth/callback/github (production)
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# NextAuth Configuration
# NEXTAUTH_URL should match your application's URL
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# NEXTAUTH_SECRET: A random secret used to encrypt tokens and email verification hashes
# Generate with: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# IMPORTANT: Use different secrets for development and production!
NEXTAUTH_SECRET=your_nextauth_secret_here_at_least_32_characters_long

# Development vs Production Notes:
# 
# For Development:
# - NEXTAUTH_URL=http://localhost:3000
# - GitHub OAuth callback: http://localhost:3000/api/auth/callback/github
# 
# For Production:
# - NEXTAUTH_URL=https://yourdomain.com
# - GitHub OAuth callback: https://yourdomain.com/api/auth/callback/github
# - Generate a NEW NEXTAUTH_SECRET (never reuse development secrets)
# - Update GitHub OAuth app settings with production URLs